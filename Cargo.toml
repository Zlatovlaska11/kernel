[workspace]
members = ["kernel"]
resolver = "2"

[package]
name = "runner"
version = "0.1.0"
edition = "2021"

[dependencies]

[build-dependencies]
bootloader = { version = "0.11", default-features = false, features = ["uefi"] }
ovmf-prebuilt = "0.2"
